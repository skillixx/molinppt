# Template Module

The template foundation stores and retrieves template metadata.

Current implementation:

- `src/templates.js`
- list templates
- get one template by ID

Future work:

- persisted template catalog
- template preview assets
- compatibility rules by deck type and slide count
